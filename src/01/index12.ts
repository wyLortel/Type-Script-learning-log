const printHello = function printHello(): void {
  console.log('Hello');
};

const add = function add(n1: number, n2: number): number {
  return n1 + n2;
};

const greet = function greet(name: string): string {
  return 'Hello,' + name;
};
